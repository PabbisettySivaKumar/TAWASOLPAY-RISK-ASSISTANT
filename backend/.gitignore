# Secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Sentence-transformers model cache (will redownload)
.cache/
~/.cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
logs/

# Local data that shouldn't be committed
# (Note: data/raw/ CSVs ARE committed for the assignment)
# (Note: data/chroma_db/ IS committed so HF Spaces doesn't rebuild on every cold start)
data/temp/

# Backup files — never commit, but keep the folder via its README
data/backups/*
!data/backups/README.md
