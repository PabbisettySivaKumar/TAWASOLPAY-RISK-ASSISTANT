# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12   # for FastAPI's UploadFile / form data

# Data handling
pandas==2.2.3
numpy==2.1.2

# HTTP client (for fetching CISA KEV + NIST 800-53)
requests==2.32.3
httpx==0.27.2

# RAG stack
chromadb==0.5.15
sentence-transformers==3.2.1

# LLM router (Gemini primary, Groq fallback)
litellm==1.51.0

# Config + validation
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# Testing (dev only)
pytest==8.3.3
pytest-asyncio==0.24.0
