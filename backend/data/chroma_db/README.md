# `data/chroma_db/`

Persistent ChromaDB vector store built by `scripts/setup_data.py`.

**Committed to Git on purpose** — so a fresh clone (or a cold start on
Hugging Face Spaces) doesn't have to rebuild from scratch.

Re-run `python scripts/setup_data.py` to rebuild.
